# SoldierTools PWA

This is the SoldierTools Progressive Web App build.

On iPhone: host over HTTPS, open in Safari, Share → Add to Home Screen → enable Open as Web App → Add.

The website and installed home-screen app use the same project, so future web updates can be delivered to the installed app.
